# docker-react-mycomponents
Basic *React* components with `semantic-ui` and `faker.js`. Also used `vs code` and `docker` *dev container*. Part of learning React following **Stephen Grider**'s tutorials and blogs. :melon:
